package ENTREGABLES.clases;

public class cadenasOstrings {
    public static void main(String[] args) {

        //String(String s) = crea nuevo string copiando el que recibe

       // String (char[] v) = crea un estring y le asigna como valor los caracteres del array

        //String (cha[] v, 1, 3) = igual que anterior pero desde una posicion hasta otra

          //      METODOS

        // a.lengt = longitud de cadena
        // a.indexOf('caracter') = devuelve primera posicion de aparcicion de ese caracter
        // a.lastIndexOf('caracter') = devuelve la posicion de ultima aparicion del caracter
        // a.charAt(n) = devuelve el caracte de la posicion n
        // a.substring(n1,n2) = devuelve subcadena entre n1 y n2
        // a.toUpperCase() = convierte a mayuscula
        // a.toLowerCase() = convierte a minuscula
        // a.equals("cad") = compara 2 cadenas devuelve true si son iguales
        // a.equalsIgnoreCase(cad) = igual sin considerar mayusculas
        // a.compareTo(otro string) = devuelve 0 si son iguales, <0 alfabeticamente menor >0 si mayor
        // a.compareToIgnoreCase(otro string) = igual pero sin mayusculas
        // a.valueOf(n) = Convierte el valor n a String n puede ser cualquier tipo
    }
}
