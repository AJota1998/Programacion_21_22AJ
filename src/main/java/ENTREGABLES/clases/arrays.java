package ENTREGABLES.clases;

import java.util.Arrays;

public class arrays {
    public static void main(String[] args) {

        // METODOS

        // Arrays.binarySearch(a, 21) = busca en el array a y devuelve otro array con la posicion del 21
        // Arrays.toString(a) = devuelve una cadena con el contenido del array
        // Arrays.copyOf(a) = copia el array en un nuevo array
        // Arrays.equals(a, b) = indica si dos arrays son iguales
        // Arrays.copyOf(a, new lenght) = añade un nuevo espacio al array indicado

        // Un array puede ser de enteros, de String, etc.
    }
}
