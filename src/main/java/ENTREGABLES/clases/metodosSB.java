package ENTREGABLES.clases;

public class metodosSB {
    public static void main(String[] args) {

        // StringBuilder(string) = construye builder a partir del string que se le pasa
        // StringBuilder(int capacidad) = por defecto tiene 16 caracteres, si queremos más debemos indicarlo

        // Metodos

        // a.append(...) = añade al final del SB un string o su representacion de un dato asociado a una v. primitiva
        // a.capacity() = devuelve la capacidad del SB
        // a.length() = devuelve la longitud
        // a.reverse() = invierte el orden de los caracteres del SB
        // a.setCharAt(int indice, char "") = cambia el caracter del indice por el char
        // a.setLength(int nueva L) = modifica la longitud, la longitud nueva no puede ser menor
        // a.toString() = de SB a String
        // a.insert(int indice1, String cadena) = añade la cadena a partir del indice
        // a.delete(int indice1, int indice fin) = borra la cadena incluida entre los indices
        // a.deleteChar(int indice) = Borra el caracter indicado en el indice
        // a.replace(int indice1, int indice final, String b) = reemplaza los comprendidos por el string
        // a.indexOf(String b) analiza los caracteres de la cadena y encuentra el primer indice que coincide con el valor deseado
        // a.subString(int indice1, int indice fin) = devuelve cadena comprendida entre 2 indices
    }
}
